

#### Remember!

- Volumes are not handled by volume driver plugins
- The local volume plugin helps create a volume on the Docker host and stores its data
- When you run a Docker container you can choose to use a specific volume driver
	- When container exits, your data is safe on the cloud